

const ActiveButton = ({btnTitle}) => {
    return (
        <div>
            <button className="px-5 py-3 bg-[#F7A582] text-white rounded-lg font-bold">{btnTitle}</button>
        </div>
    );
};

export default ActiveButton;